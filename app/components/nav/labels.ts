// labels.ts — 화면 이름의 단일 출처.
//
// 왜 파일로 뺐나: 같은 화면 이름이 하단 탭바·화면 제목·메뉴 항목에 각각 문자열
// 리터럴로 박혀 있어 반복해서 어긋났다. 결산 탭은 탭 라벨이 "수익 및 세금"인데
// 화면 제목만 "결산"이었고, 메뉴의 "내 정보"도 다른 곳과 표현이 갈렸다.
// 이름을 바꿀 일이 생기면 여기 한 곳만 고친다.
//
// 규칙: 하단 탭 라벨은 3분할이라 폭이 좁다 — 되도록 짧게 두고, 화면 제목과
// 반드시 같은 문자열을 쓴다. 다르게 보여야 할 이유가 생기면 그때 별도 키를 만든다.

export const SCREEN = {
  portfolio: "포트폴리오",
  settlement: "결산",
  simulator: "은퇴시뮬레이션",
  settings: "설정",
} as const;

export type ScreenKey = keyof typeof SCREEN;
