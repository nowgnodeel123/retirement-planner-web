# retirement-planner-web (프론트)

이 파일은 이 레포 전용 규칙이다. 워크스페이스 공통 규칙은 상위 폴더 `../CLAUDE.md`를 함께 참조할 것.

## 스택

Next.js 15 / TypeScript / Tailwind CSS. App Router.

## 구조

App Router 세그먼트 유지, 페이지는 조립만. 공용 컴포넌트는 기능 중립 이름으로 `app/components/` 아래에 둔다.
(백로그: `app/components/wizard/Ui.tsx`를 기능 중립적 위치인 `app/components/ui/`로 이동 예정)

## 서버/클라이언트 컴포넌트

서버 컴포넌트 기본. 상태·이벤트·브라우저 API가 필요한 지점에만 `'use client'`.

## 타입

백엔드 응답 타입은 한 곳에 정의. **백엔드 DTO 필드가 바뀌면 같은 세션에서 이 타입도 반영한다** — "프론트 반영까지"가 한 작업 단위. 가격·환율 필드는 항상 nullable로 정의(시세 조회 실패 시 degrade 대응).

## 통신

fetch는 API 클라이언트 계층을 경유(인증 토큰 주입 지점 단일화). 별도 상태관리 라이브러리 도입 금지 — 현재 규모에 React 기본으로 충분.

## 표시 규칙

- 이익=빨강, 손실=파랑 (서구권 반대 관례 금지)
- 카테고리 색상에 순수 빨강·파랑 금지 (손익 색상과 혼동 방지)
- 금액은 3자리 콤마 + 통화기호
- 해외주식은 원화 병기하되, 손익률은 원통화(USD) 기준 하나만 표시
- 국내주식엔 "전일 종가 기준" 라벨 상시 표시
- 모바일 웹이 1차 표면 — 터치타겟 크기, hover 비의존 인터랙션 기본

## 다크모드

전역 CSS 변수 토큰 사용 (neutral-*/blue-*/white 하드코딩 금지). 히어로(파란 그라데이션) 카드는 의도된 고정 강조색이라 예외. 경고(호박색)는 `--warning`/`--warning-soft` 토큰 사용(`--error`와 동일 패턴, S-042 세션에서 추가).

## 임시 코드

`DevTokenGate`는 실제 로그인(M12)으로 전량 대체될 임시 장치. 여기에 기능을 추가하지 않는다.

## 정렬 UI 컴포넌트 (M7, D-094)

`app/components/portfolio/SortModal.tsx` — 이름은 Modal이지만 실제 구현은 앵커드 드롭다운. 정렬 버튼 바로 아래, 5개 결합 옵션(평가금액/수익률/이름 × 방향) 단일 리스트, 선택 즉시 적용, 바깥 클릭·ESC로 닫힘.
